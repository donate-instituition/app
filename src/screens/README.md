# Telas

Telas reutilizaveis devem ficar em `src/screens`.

Com Expo Router, os arquivos em `src/app/` representam rotas. Quando uma tela crescer, deixe o arquivo de rota fino e delegue a interface para `src/screens/<feature>`.
