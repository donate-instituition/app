# Hooks

Hooks reutilizaveis da aplicacao devem ficar aqui.

Hooks ligados a uma unica funcionalidade devem ficar perto da feature correspondente.

Use `use-screen-state` para padronizar a decisao entre loading, erro, vazio, sucesso e conteudo.
