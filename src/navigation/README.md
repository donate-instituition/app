# Navegacao

O projeto usa Expo Router para registrar rotas dentro de `src/app/`.

Este diretorio guarda configuracoes compartilhadas de navegacao, como temas do React Navigation, helpers de rota e tipos globais quando necessario.
